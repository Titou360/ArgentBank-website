// État initial
const initialState = {
    dealDetails: {},
  };

  // Reducer
const dealReducer = (state = initialState, action) => {
    switch (action.type) {
      default:
        return state;
    }
  };

  export default dealReducer